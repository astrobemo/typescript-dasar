describe('Data Types', () => {
    it('should be able to infer types', () => {

        const name: string = 'John Doe';
        const age: number = 25;
        const isStudent: boolean = false;

        console.info(name);
        
        console.info(age);
        console.info(isStudent);
        
    });
})